[inbound]

exten => _X.,1,Noop(Inbound context)
same =>      n,Answer()
same =>      n,Wait(5)
same =>      n,Hangup()

exten => service,1,Noop(SIPP Inbound)
same =>      n,Answer()
same =>      n,Wait(5)
same =>      n,Hangup()
